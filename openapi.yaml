openapi: 3.1.0
info:
  title: Alliora
  version: 0.1.0
  description: >
    Canonical HTTP contracts live in packages/openapi-core/src/
    (one YAML file per domain: identity, capabilities, decisions,
    stagegates, experience, dependencies, alerts, workforce, reports).
    This file is a pointer only — do not add paths here.
servers:
  - url: http://127.0.0.1:4000
    description: Local API
paths: {}
