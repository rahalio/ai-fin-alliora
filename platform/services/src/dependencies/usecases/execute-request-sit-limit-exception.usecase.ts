/**
 * ExecuteRequestSitLimitException - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dependencies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRequestSitLimitException Use Case
 *
 * DDD: Application use case for get SitException.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RequestSitLimitExceptionInput, RequestSitLimitExceptionOutput } from "../dto/sit-exception.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { SitExceptionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRequestSitLimitException {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly sitException: SitExceptionRepository
  ) {}

  async execute(input: RequestSitLimitExceptionInput): Promise<RequestSitLimitExceptionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.sitId();

    // All field names extracted from OpenAPI spec: dependencyId, rationale
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).dependencyId) {
      throw new ValidationError("dependencyId is required");
    }
    const dependencyId = (input as any).dependencyId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.sitException.requestSitLimitException({ dependencyId: dependencyId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${dependencyId}`);
    }

    return entity as RequestSitLimitExceptionOutput;
  }
}
