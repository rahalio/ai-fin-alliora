/**
 * ExecuteRaisePlatformRiskAlert - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRaisePlatformRiskAlert Use Case
 *
 * DDD: Application use case for get Alert.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RaisePlatformRiskAlertInput, RaisePlatformRiskAlertOutput } from "../dto/alert.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { AlertRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRaisePlatformRiskAlert {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly alert: AlertRepository
  ) {}

  async execute(input: RaisePlatformRiskAlertInput): Promise<RaisePlatformRiskAlertOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.altId();

    // All field names extracted from OpenAPI spec: capabilityId, detail, interfacePowerSignal, severity
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).capabilityId) {
      throw new ValidationError("capabilityId is required");
    }
    const capabilityId = (input as any).capabilityId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.alert.raisePlatformRiskAlert({ ...input, capabilityId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${capabilityId}`);
    }

    return entity as RaisePlatformRiskAlertOutput;
  }
}
