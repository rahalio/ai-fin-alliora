/**
 * ExecuteAttachLiabilityTerms - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: experience
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAttachLiabilityTerms Use Case
 *
 * DDD: Application use case for get Liability.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AttachLiabilityTermsInput, AttachLiabilityTermsOutput } from "../dto/liability.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { LiabilityRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAttachLiabilityTerms {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly liability: LiabilityRepository
  ) {}

  async execute(input: AttachLiabilityTermsInput): Promise<AttachLiabilityTermsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.expId();

    // All field names extracted from OpenAPI spec: distributorLiability, manufacturerLiability
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).mapId) {
      throw new ValidationError("mapId is required");
    }
    const mapId = (input as any).mapId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.liability.attachLiabilityTerms({ ...input, mapId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${mapId}`);
    }

    return entity as AttachLiabilityTermsOutput;
  }
}
