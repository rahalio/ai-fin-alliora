/**
 * ExecuteAttachProfitPoolTest - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decisions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAttachProfitPoolTest Use Case
 *
 * DDD: Application use case for get Test.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AttachProfitPoolTestInput, AttachProfitPoolTestOutput } from "../dto/test.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { TestRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAttachProfitPoolTest {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly test: TestRepository
  ) {}

  async execute(input: AttachProfitPoolTestInput): Promise<AttachProfitPoolTestOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.dcsId();

    // All field names extracted from OpenAPI spec: resultNotes, tested
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).hypothesisId) {
      throw new ValidationError("hypothesisId is required");
    }
    const hypothesisId = (input as any).hypothesisId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.test.attachProfitPoolTest({ hypothesisId: hypothesisId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${hypothesisId}`);
    }

    return entity as AttachProfitPoolTestOutput;
  }
}
