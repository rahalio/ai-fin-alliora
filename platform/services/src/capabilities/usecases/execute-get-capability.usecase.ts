/**
 * ExecuteGetCapability - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: capabilities
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetCapability Use Case
 *
 * DDD: Application use case for get Capability.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetCapabilityInput, GetCapabilityOutput } from "../dto/capability.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { CapabilityRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetCapability {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly capability: CapabilityRepository
  ) {}

  async execute(input: GetCapabilityInput): Promise<GetCapabilityOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.capId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).capabilityId) {
      throw new ValidationError("capabilityId is required");
    }
    const capabilityId = (input as any).capabilityId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.capability.getCapability({ capabilityId: capabilityId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${capabilityId}`);
    }

    return entity as GetCapabilityOutput;
  }
}
