/**
 * ExecuteTagCapabilityForces - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: capabilities
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteTagCapabilityForces Use Case
 *
 * DDD: Application use case for get ForceTag.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { TagCapabilityForcesInput, TagCapabilityForcesOutput } from "../dto/force-tag.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { ForceTagRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteTagCapabilityForces {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly forceTag: ForceTagRepository
  ) {}

  async execute(input: TagCapabilityForcesInput): Promise<TagCapabilityForcesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.capId();

    // All field names extracted from OpenAPI spec: forces, valueChainPosition
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).capabilityId) {
      throw new ValidationError("capabilityId is required");
    }
    const capabilityId = (input as any).capabilityId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.forceTag.tagCapabilityForces({ ...input, capabilityId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${capabilityId}`);
    }

    return entity as TagCapabilityForcesOutput;
  }
}
