/**
 * ExecuteDecideStageGateOutcome - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: stagegates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteDecideStageGateOutcome Use Case
 *
 * DDD: Application use case for get Outcome.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { DecideStageGateOutcomeInput, DecideStageGateOutcomeOutput } from "../dto/outcome.dto";
import type { ExecutionContextService, IdGeneratorService } from "@alliora/services/_shared/index.js";
import type { OutcomeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteDecideStageGateOutcome {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly outcome: OutcomeRepository
  ) {}

  async execute(input: DecideStageGateOutcomeInput): Promise<DecideStageGateOutcomeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gatId();

    // All field names extracted from OpenAPI spec: outcome, rationale
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).gateId) {
      throw new ValidationError("gateId is required");
    }
    const gateId = (input as any).gateId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.outcome.decideStageGateOutcome({ ...input, gateId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${gateId}`);
    }

    return entity as DecideStageGateOutcomeOutput;
  }
}
