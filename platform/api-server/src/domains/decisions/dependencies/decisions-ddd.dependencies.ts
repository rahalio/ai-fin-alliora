/**
 * Decisions DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decisions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Decisions Domain Module - Composition root
 *
 * DDD: Composition root for decisions domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  CostCompareRepositoryAdapter,
  DecisionRepositoryAdapter,
  PartnershipContractRepositoryAdapter,
  ProfitPoolHypothesisRepositoryAdapter,
  TestRepositoryAdapter,
} from "@alliora/adapters/decisions";
import { getIdGeneratorService } from "@alliora/adapters";
import type { AdapterDynamoDBClient } from "@alliora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAttachProfitPoolTest,
  ExecuteCreateCostCompare,
  ExecuteCreatePartnershipContract,
  ExecuteCreateProfitPoolHypothesis,
  ExecuteGetDecisionClass,
  ExecuteListCostCompares,
  ExecuteListDecisionClasses,
  ExecuteListPartnershipContracts,
  ExecuteListProfitPoolHypotheses,
  ExecuteRecordDecisionClass,
  ExecuteUpdatePartnershipContract,
} from "@alliora/services/decisions/usecases";
// Ports (interfaces)
import type {
  CostCompareRepository,
  DecisionRepository,
  PartnershipContractRepository,
  ProfitPoolHypothesisRepository,
  TestRepository,
} from "@alliora/services/decisions/ports";

export interface DecisionsDomainModule {
  repos: {
    costCompares: CostCompareRepository;
    decisions: DecisionRepository;
    partnershipContracts: PartnershipContractRepository;
    profitPoolHypotheses: ProfitPoolHypothesisRepository;
    tests: TestRepository;
  };
  useCases: {
    costCompares: {
      create: ExecuteCreateCostCompare;
      list: ExecuteListCostCompares;
    };
    decisions: {
      get: ExecuteGetDecisionClass;
      list: ExecuteListDecisionClasses;
      record: ExecuteRecordDecisionClass;
    };
    partnershipContracts: {
      create: ExecuteCreatePartnershipContract;
      list: ExecuteListPartnershipContracts;
      update: ExecuteUpdatePartnershipContract;
    };
    profitPoolHypotheses: {
      create: ExecuteCreateProfitPoolHypothesis;
      list: ExecuteListProfitPoolHypotheses;
    };
    tests: {
      get: ExecuteAttachProfitPoolTest;
    };
  };
}

export function buildDecisionsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): DecisionsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      costCompares?: CostCompareRepository;
      decisions?: DecisionRepository;
      partnershipContracts?: PartnershipContractRepository;
      profitPoolHypotheses?: ProfitPoolHypothesisRepository;
      tests?: TestRepository;
    } = {};

    obj.costCompares = new CostCompareRepositoryAdapter(dynamoClient);
    obj.decisions = new DecisionRepositoryAdapter(dynamoClient);
    obj.partnershipContracts = new PartnershipContractRepositoryAdapter(dynamoClient);
    obj.profitPoolHypotheses = new ProfitPoolHypothesisRepositoryAdapter(dynamoClient);
    obj.tests = new TestRepositoryAdapter(dynamoClient);
    return {
      costCompares: obj.costCompares!,
      decisions: obj.decisions!,
      partnershipContracts: obj.partnershipContracts!,
      profitPoolHypotheses: obj.profitPoolHypotheses!,
      tests: obj.tests!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    costCompares: {
      create: new ExecuteCreateCostCompare(executionContext, idGenerator, repos.costCompares),
      list: new ExecuteListCostCompares(executionContext, idGenerator, repos.costCompares),
    },
    decisions: {
      get: new ExecuteGetDecisionClass(executionContext, idGenerator, repos.decisions),
      list: new ExecuteListDecisionClasses(executionContext, idGenerator, repos.decisions),
      record: new ExecuteRecordDecisionClass(executionContext, idGenerator, repos.decisions),
    },
    partnershipContracts: {
      create: new ExecuteCreatePartnershipContract(executionContext, idGenerator, repos.partnershipContracts),
      list: new ExecuteListPartnershipContracts(executionContext, idGenerator, repos.partnershipContracts),
      update: new ExecuteUpdatePartnershipContract(executionContext, idGenerator, repos.partnershipContracts),
    },
    profitPoolHypotheses: {
      create: new ExecuteCreateProfitPoolHypothesis(executionContext, idGenerator, repos.profitPoolHypotheses),
      list: new ExecuteListProfitPoolHypotheses(executionContext, idGenerator, repos.profitPoolHypotheses),
    },
    tests: {
      get: new ExecuteAttachProfitPoolTest(executionContext, idGenerator, repos.tests),
    },
  };
  return { repos, useCases: useCases as DecisionsDomainModule["useCases"] };
}
