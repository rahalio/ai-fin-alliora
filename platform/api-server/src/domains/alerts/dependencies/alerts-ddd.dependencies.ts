/**
 * Alerts DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Alerts Domain Module - Composition root
 *
 * DDD: Composition root for alerts domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AcknowledgeRepositoryAdapter,
  AlertRepositoryAdapter,
  EscalateRepositoryAdapter,
} from "@alliora/adapters/alerts";
import { getIdGeneratorService } from "@alliora/adapters";
import type { AdapterDynamoDBClient } from "@alliora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAcknowledgePlatformRiskAlert,
  ExecuteEscalatePlatformRiskAlert,
  ExecuteGetPlatformRiskAlert,
  ExecuteListPlatformRiskAlerts,
  ExecuteRaisePlatformRiskAlert,
} from "@alliora/services/alerts/usecases";
// Ports (interfaces)
import type {
  AcknowledgeRepository,
  AlertRepository,
  EscalateRepository,
} from "@alliora/services/alerts/ports";

export interface AlertsDomainModule {
  repos: {
    acknowledges: AcknowledgeRepository;
    alerts: AlertRepository;
    escalates: EscalateRepository;
  };
  useCases: {
    acknowledges: {
      get: ExecuteAcknowledgePlatformRiskAlert;
    };
    alerts: {
      get: ExecuteGetPlatformRiskAlert;
      list: ExecuteListPlatformRiskAlerts;
      raise: ExecuteRaisePlatformRiskAlert;
    };
    escalates: {
      get: ExecuteEscalatePlatformRiskAlert;
    };
  };
}

export function buildAlertsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): AlertsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      acknowledges?: AcknowledgeRepository;
      alerts?: AlertRepository;
      escalates?: EscalateRepository;
    } = {};

    obj.acknowledges = new AcknowledgeRepositoryAdapter(dynamoClient);
    obj.alerts = new AlertRepositoryAdapter(dynamoClient);
    obj.escalates = new EscalateRepositoryAdapter(dynamoClient);
    return {
      acknowledges: obj.acknowledges!,
      alerts: obj.alerts!,
      escalates: obj.escalates!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    acknowledges: {
      get: new ExecuteAcknowledgePlatformRiskAlert(executionContext, idGenerator, repos.acknowledges),
    },
    alerts: {
      get: new ExecuteGetPlatformRiskAlert(executionContext, idGenerator, repos.alerts),
      list: new ExecuteListPlatformRiskAlerts(executionContext, idGenerator, repos.alerts),
      raise: new ExecuteRaisePlatformRiskAlert(executionContext, idGenerator, repos.alerts),
    },
    escalates: {
      get: new ExecuteEscalatePlatformRiskAlert(executionContext, idGenerator, repos.escalates),
    },
  };
  return { repos, useCases: useCases as AlertsDomainModule["useCases"] };
}
