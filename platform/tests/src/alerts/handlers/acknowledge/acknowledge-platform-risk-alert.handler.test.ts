/**
 * AcknowledgePlatformRiskAlert handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { acknowledgePlatformRiskAlert } from "@alliora/api-server/domains/alerts/handlers";
import type { AlertsDDDDependencies } from "@alliora/api-server/domains/alerts";
import { createAcknowledge as createAcknowledgeEntity } from "@alliora/factories/alerts";
import { createCreateAcknowledgeRequest } from "@alliora/factories/alerts";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { ALERTS_USE_CASES } from "../__factories__/use-cases.js";

describe("AcknowledgePlatformRiskAlert", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AlertsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AlertsDDDDependencies>(
      ALERTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateAcknowledgeRequest();
    const entity = createAcknowledgeEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeAcknowledgePlatformRiskAlert, "execute").mockResolvedValue(useCaseResult);

    await acknowledgePlatformRiskAlert(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeAcknowledgePlatformRiskAlert.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
