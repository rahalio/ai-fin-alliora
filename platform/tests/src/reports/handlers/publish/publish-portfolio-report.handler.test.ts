/**
 * PublishPortfolioReport handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { publishPortfolioReport } from "@alliora/api-server/domains/reports/handlers";
import type { ReportsDDDDependencies } from "@alliora/api-server/domains/reports";
import { createPublish as createPublishEntity } from "@alliora/factories/reports";
import { createCreatePublishRequest } from "@alliora/factories/reports";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { REPORTS_USE_CASES } from "../__factories__/use-cases.js";

describe("PublishPortfolioReport", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReportsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReportsDDDDependencies>(
      REPORTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreatePublishRequest();
    const entity = createPublishEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executePublishPortfolioReport, "execute").mockResolvedValue(useCaseResult);

    await publishPortfolioReport(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executePublishPortfolioReport.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
