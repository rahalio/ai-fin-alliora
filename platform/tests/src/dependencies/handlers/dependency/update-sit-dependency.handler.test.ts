/**
 * UpdateSitDependency handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dependencies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateSitDependency } from "@alliora/api-server/domains/dependencies/handlers";
import type { DependenciesDDDDependencies } from "@alliora/api-server/domains/dependencies";
import { createDependency as createDependencyEntity } from "@alliora/factories/dependencies";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { DEPENDENCIES_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateSitDependency", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DependenciesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DependenciesDDDDependencies>(
      DEPENDENCIES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createDependencyEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { dependencyId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateSitDependency, "execute").mockResolvedValue(useCaseResult);

    await updateSitDependency(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateSitDependency.execute).toHaveBeenCalledWith(expect.objectContaining({ dependencyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
