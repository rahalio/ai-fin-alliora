/**
 * GetSitSpendBlock handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: dependencies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSitSpendBlock } from "@alliora/api-server/domains/dependencies/handlers";
import type { DependenciesDDDDependencies } from "@alliora/api-server/domains/dependencies";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { DEPENDENCIES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSitSpendBlock", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DependenciesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DependenciesDDDDependencies>(
      DEPENDENCIES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSitSpendBlock, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { dependencyId: entity.id };

    await getSitSpendBlock(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSitSpendBlock.execute).toHaveBeenCalledWith(expect.objectContaining({ dependencyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { dependencyId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSitSpendBlock, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSitSpendBlock(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
