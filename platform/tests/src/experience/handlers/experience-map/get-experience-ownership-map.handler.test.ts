/**
 * GetExperienceOwnershipMap handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: experience
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getExperienceOwnershipMap } from "@alliora/api-server/domains/experience/handlers";
import type { ExperienceDDDDependencies } from "@alliora/api-server/domains/experience";
import { createExperienceMap as createExperienceMapEntity } from "@alliora/factories/experience";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { EXPERIENCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetExperienceOwnershipMap", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ExperienceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ExperienceDDDDependencies>(
      EXPERIENCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createExperienceMapEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetExperienceOwnershipMap, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { mapId: entity.id };

    await getExperienceOwnershipMap(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetExperienceOwnershipMap.execute).toHaveBeenCalledWith(expect.objectContaining({ mapId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { mapId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetExperienceOwnershipMap, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getExperienceOwnershipMap(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
