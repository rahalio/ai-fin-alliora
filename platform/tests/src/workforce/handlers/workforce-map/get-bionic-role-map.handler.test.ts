/**
 * GetBionicRoleMap handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: workforce
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getBionicRoleMap } from "@alliora/api-server/domains/workforce/handlers";
import type { WorkforceDDDDependencies } from "@alliora/api-server/domains/workforce";
import { createWorkforceMap as createWorkforceMapEntity } from "@alliora/factories/workforce";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@alliora/tests/shared";
import { WORKFORCE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetBionicRoleMap", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: WorkforceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<WorkforceDDDDependencies>(
      WORKFORCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createWorkforceMapEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetBionicRoleMap, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { mapId: entity.id };

    await getBionicRoleMap(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetBionicRoleMap.execute).toHaveBeenCalledWith(expect.objectContaining({ mapId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { mapId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetBionicRoleMap, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getBionicRoleMap(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
